env_id: SysAdmin-RL
version: "0.1.0"
entry_point: server.app:main
sdk: docker
action_space:
  type: discrete
  description: "Commands to fix the server (e.g., restart_service)"
observation_space:
  type: text
  description: "Terminal output and system status"
