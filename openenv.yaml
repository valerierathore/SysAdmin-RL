env_id: SysAdmin-RL
version: "0.1.0"
entry_point: server.app:main
sdk: docker

tasks:
  - id: easy
    description: "Restart service task"
    grader:
      type: function
      name: graders.easy_grader

  - id: medium
    description: "Clean disk task"
    grader:
      type: function
      name: graders.medium_grader

  - id: hard
    description: "Unblock port task"
    grader:
      type: function
      name: graders.hard_grader
